add_namespace = secret_cheats

country_event = {
    id = secret_cheats.1
    title = secret_cheats.1.t
    desc = secret_cheats.1.d
    is_triggered_only = yes

    option = {
        name = secret_cheats.1.a
        create_corps_commander = {
            name = "Biggus Dickus"
            desc = "POLITICS_BIGGUS_DICKUS_DESC"
            picture = "GFX_portrait_generic_male"
            skill = 10
            attack_skill = 10
            defense_skill = 10
            planning_skill = 10
            logistics_skill = 10
            traits = {
                brilliant_strategist
                offensive_doctrine
                defensive_doctrine
                logistics_wizard
                adaptable
                panzer_leader
                commando
            }
        }
    }
}

country_event = {
    id = secret_cheats.2
    title = secret_cheats.2.t
    desc = secret_cheats.2.d
    is_triggered_only = yes

    option = {
        name = secret_cheats.2.a

        create_corps_commander = {
            name = "Steiner at the Rescue"
            desc = "POLITICS_STEINER_RESCUE_DESC"
            picture = "GFX_portrait_generic_male"
            skill = 7
            attack_skill = 9
            defense_skill = 7
            planning_skill = 8
            logistics_skill = 6
            traits = {
                panzer_leader
                aggressive_assaulter
                adaptable
            }
        }

        add_ideas = secret_cheat_steiner_rescue
    }
}

country_event = {
    id = secret_cheats.100
    hidden = yes
    is_triggered_only = yes

    immediate = {
        random_owned_controlled_state = {
            limit = { is_core_of = ROOT }
            add_resource = { type = steel amount = 40 }
        }
    }
}

country_event = {
    id = secret_cheats.101
    hidden = yes
    is_triggered_only = yes

    immediate = {
        random_owned_controlled_state = {
            limit = { is_core_of = ROOT }
            add_resource = { type = aluminium amount = 20 }
        }
    }
}

country_event = {
    id = secret_cheats.102
    hidden = yes
    is_triggered_only = yes

    immediate = {
        random_owned_controlled_state = {
            limit = { is_core_of = ROOT }
            add_resource = { type = tungsten amount = 10 }
        }
    }
}

country_event = {
    id = secret_cheats.103
    hidden = yes
    is_triggered_only = yes

    immediate = {
        random_owned_controlled_state = {
            limit = { is_core_of = ROOT }
            add_resource = { type = oil amount = 40 }
        }
    }
}

country_event = {
    id = secret_cheats.104
    hidden = yes
    is_triggered_only = yes

    immediate = {
        random_owned_controlled_state = {
            limit = { is_core_of = ROOT }
            add_resource = { type = chromium amount = 20 }
        }
    }
}

country_event = {
    id = secret_cheats.105
    hidden = yes
    is_triggered_only = yes

    immediate = {
        random_owned_controlled_state = {
            limit = { is_core_of = ROOT }
            add_resource = { type = rubber amount = 20 }
        }
    }
}
